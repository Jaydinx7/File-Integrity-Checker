# File-Integrity-Checker

A lightweight command-line tool built in Java to verify data integrity using the SHA-256 cryptographic hashing algorithm.

## How to Run
1. Compile the script: `javac IntegrityChecker.java`
2. Run the script: `java IntegrityChecker`
3. Enter the absolute path to the file you want to verify.